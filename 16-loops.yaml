- name: loops demo
  hosts: local
  connection: local
  tasks:
  - name: print the names
    ansible.builtin.debug:
      msg: " Hello {{ item }}"
    loop:
    - Ramana
    - Raju
    - Rakesh
    - Rajesh
    - Ram